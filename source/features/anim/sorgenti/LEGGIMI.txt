sgp_chunk.h
    copia non modificata di SGP-1.2-QUALITA-NATIVO-01/sorgenti-v-finale/sgp_chunk.h:
    la fonte unica della pianta del chunk e dell'accessore `sgp_chunk_opzione`.
sgp_anim4.h / anim_idle4.c / anim_blob4.c
    la v4 di questo cantiere: e' la versione che va in ROM. La copia dei
    sorgenti della v3 (SGP-1.2-ANIM-B-03), tenuta qui per un confronto storico,
    non e' piu' distribuita; le differenze volute sono elencate una per una in
    testa a `sgp_anim4.h`, `anim_idle4.c` e `tools/compila_anim4.py`.
